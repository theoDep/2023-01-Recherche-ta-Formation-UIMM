<form method="post" action="{{ path('app_format_type_delete', {'id': format.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ format.id) }}">
    <button class="btn">Delete</button>
</form>
