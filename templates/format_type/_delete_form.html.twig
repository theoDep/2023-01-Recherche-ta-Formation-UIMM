<form method="post" action="{{ path('app_format_type_delete', {'id': format.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette format ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ format.id) }}">
    <button class="btn mt-2"><img src="{{asset('build/img/trash-icon.svg')}}" alt="icon poubelle" title="Supprimer"></button>
</form>
