{% extends 'backoffice_base.html.twig' %}

{% block title %}New Format{% endblock %}

{% block body %}
    <div class="page" data-current="format"></div>
    <h1>Create new Format</h1>

    {{ include('format_type/_form.html.twig') }}

    <a href="{{ path('app_format_type_index') }}">back to list</a>

    {% block javascripts %}
        {{ encore_entry_script_tags('backoffice') }}
        {{parent()}}
    {% endblock %}
{% endblock %}
