<form method="post" action="{{ path('app_code_type_delete', {'id': code.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ code.id) }}">
    <button class="btn">Delete</button>
</form>
