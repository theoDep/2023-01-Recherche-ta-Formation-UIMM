<form method="post" action="{{ path('app_code_type_delete', {'id': code.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce code ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ code.id) }}">
    <button class="btn mt-2"><img src="{{asset('build/img/trash-icon.svg')}}" alt="icon poubelle" title="Supprimer"></button>
</form>
