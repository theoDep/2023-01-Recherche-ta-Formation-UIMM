{% extends 'base.html.twig' %}

{% block title %}Code index{% endblock %}

{% block body %}
    <h1>Code index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Identifier</th>
                <th>Title</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for code in codes %}
            <tr>
                <td>{{ code.id }}</td>
                <td>{{ code.identifier }}</td>
                <td>{{ code.title }}</td>
                <td>
                    <a href="{{ path('app_code_type_show', {'id': code.id}) }}">show</a>
                    <a href="{{ path('app_code_type_edit', {'id': code.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_code_type_new') }}">Create new</a>
{% endblock %}
