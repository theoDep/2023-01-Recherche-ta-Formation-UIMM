{% extends 'backoffice_base.html.twig' %}

{% block title %}Skill index{% endblock %}

{% block body %}
    <div class="page" data-current="competences"></div>
    <div class="d-flex justify-content-between mb-5">
        <h1>Compétences</h1>
        <a href="{{ path('app_skill_type_new') }}"><img src="{{asset('build/img/plus-icon.svg')}}" alt="icon plus" title="Ajouter une compétence"></a>
    </div>

    <table class="table">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Description</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for skill in skills %}
            <tr>
                <td>{{ skill.name }}</td>
                <td>{{ skill.descriptive }}</td>
                <td>
                    <div class="d-flex justify-content-around">
                        <a href="{{ path('app_skill_type_show', {'id': skill.id}) }}"><img src="{{asset('build/img/show-icon.svg')}}" class="me-2" alt="icon show" title="Voir l'entité"></a>
                        <a href="{{ path('app_skill_type_edit', {'id': skill.id}) }}"><img src="{{asset('build/img/edit-icon.svg')}}" alt="icon edit" title="Modifier l'entité"></a>
                    </div>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {% block javascripts %}
        {{ encore_entry_script_tags('backoffice') }}
        {{parent()}}
    {% endblock %}
{% endblock %}
