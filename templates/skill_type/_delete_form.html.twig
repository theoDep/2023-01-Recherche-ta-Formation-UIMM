<form method="post" action="{{ path('app_skill_type_delete', {'id': skill.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette compétence ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ skill.id) }}">
    <button class="btn mt-2"><img src="{{asset('build/img/trash-icon.svg')}}" alt="icon poubelle" title="Supprimer"></button>
</form>
