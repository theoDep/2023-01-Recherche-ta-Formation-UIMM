{% extends 'base.html.twig' %}

{% block title %}Skill{% endblock %}

{% block body %}
    <h1>Skill</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ skill.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ skill.name }}</td>
            </tr>
            <tr>
                <th>Descriptive</th>
                <td>{{ skill.descriptive }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_skill_type_index') }}">back to list</a>

    <a href="{{ path('app_skill_type_edit', {'id': skill.id}) }}">edit</a>

    {{ include('skill_type/_delete_form.html.twig') }}
{% endblock %}
