{% extends 'backoffice_base.html.twig' %}

{% block title %}New Skill{% endblock %}

{% block body %}
    <div class="page" data-current="competences"></div>
    <div class="d-flex justify-content-between mb-5 col-lg-10">
        <h1>Créer une nouvelle compétence</h1>
        <a class="mt-3" href="{{ path('app_skill_type_index') }}"><img src="{{asset('build/img/back-icon.svg')}}" alt="icon back" title="Retour en arrière"></a>
    </div>

    {{ include('skill_type/_form.html.twig') }}

    {% block javascripts %}
        {{ encore_entry_script_tags('backoffice') }}
        {{parent()}}
    {% endblock %}
{% endblock %}
