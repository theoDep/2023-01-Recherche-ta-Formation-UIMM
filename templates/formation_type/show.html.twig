{% extends 'base.html.twig' %}

{% block title %}Formation{% endblock %}

{% block body %}
    <h1>Formation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formation.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ formation.name }}</td>
            </tr>
            <tr>
                <th>Studies_level</th>
                <td>{{ formation.studiesLevel }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_formation_type_index') }}">back to list</a>

    <a href="{{ path('app_formation_type_edit', {'id': formation.id}) }}">edit</a>

    {{ include('formation_type/_delete_form.html.twig') }}
{% endblock %}
