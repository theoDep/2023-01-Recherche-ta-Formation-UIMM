{% extends 'base.html.twig' %}

{% block title %}Condition{% endblock %}

{% block body %}
    <h1>Condition</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ condition.id }}</td>
            </tr>
            <tr>
                <th>Descriptive</th>
                <td>{{ condition.descriptive }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_condition_type_index') }}">back to list</a>

    <a href="{{ path('app_condition_type_edit', {'id': condition.id}) }}">edit</a>

    {{ include('condition_type/_delete_form.html.twig') }}
{% endblock %}
