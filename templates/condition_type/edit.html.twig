{% extends 'base.html.twig' %}

{% block title %}Edit Condition{% endblock %}

{% block body %}
    <h1>Edit Condition</h1>

    {{ include('condition_type/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_condition_type_index') }}">back to list</a>

    {{ include('condition_type/_delete_form.html.twig') }}
{% endblock %}
