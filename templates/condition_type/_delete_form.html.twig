<form method="post" action="{{ path('app_condition_type_delete', {'id': condition.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette condition ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ condition.id) }}">
    <button class="btn mt-2"><img src="{{asset('build/img/trash-icon.svg')}}" alt="icon poubelle" title="Supprimer"></button>
</form>
