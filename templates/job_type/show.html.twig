{% extends 'backoffice_base.html.twig' %}

{% block title %}Job{% endblock %}

{% block body %}
    <div class="page" data-current="metiers"></div>
    <h1>Job</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ job.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ job.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_job_type_index') }}">back to list</a>

    <a href="{{ path('app_job_type_edit', {'id': job.id}) }}"><img src="{{asset('build/img/edit-icon.svg')}}" alt="icon edit" title="Modifier l'entité"></a>

    {{ include('job_type/_delete_form.html.twig') }}

    {% block javascripts %}
        {{ encore_entry_script_tags('backoffice') }}
        {{parent()}}
    {% endblock %}
{% endblock %}
