<form method="post" action="{{ path('app_cost_type_delete', {'id': cost.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce frais ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cost.id) }}">
    <button class="btn mt-2"><img src="{{asset('build/img/trash-icon.svg')}}" alt="icon poubelle" title="Supprimer"></button>
</form>
