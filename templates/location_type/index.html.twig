{% extends 'base.html.twig' %}

{% block title %}Location index{% endblock %}

{% block body %}
    <h1>Location index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Line_1</th>
                <th>Line_2</th>
                <th>City</th>
                <th>Zip_code</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for location in locations %}
            <tr>
                <td>{{ location.id }}</td>
                <td>{{ location.line1 }}</td>
                <td>{{ location.line2 }}</td>
                <td>{{ location.city }}</td>
                <td>{{ location.zipCode }}</td>
                <td>
                    <a href="{{ path('app_location_type_show', {'id': location.id}) }}">show</a>
                    <a href="{{ path('app_location_type_edit', {'id': location.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_location_type_new') }}">Create new</a>
{% endblock %}
